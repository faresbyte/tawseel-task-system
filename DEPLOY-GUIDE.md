# 🌐 كيف تحول موقعك إلى "موقع حي" للأبد؟

لتحويل المشروع من جهازك إلى الإنترنت، اتبع هذه الخطوات الـ 3 بدقة. العملية ستستغرق حوالي 15 دقيقة فقط.

---

## الخطوة 1: إنشاء "المخ" (قاعدة البيانات) 🧠

1. اذهب إلى [supabase.com](https://supabase.com)
2. سجل حسابك (Sign Up) مجاناً.
3. اضغط **New Project**
   - اختر اسماً للمشروع (مثلاً: `tawseel-one`)
   - ضع كلمة مرور قوية **واحفظها**.
   - اختر المنطقة (Region): الأفضل `Frankfurt` (ألمانيا) لقربها وسرعتها.
4. انتظر دقيقة ليتم الإنشاء.
5. من القائمة اليسرى، اختر **SQL Editor**.
6. انسخ محتوى ملف `database-schema.sql` (الموجود في مشروعك) والصقه في المحرر، ثم اضغط **Run**.
7. اذهب إلى **Settings > API**.
   - انسخ رابط الـ `URL`
   - انسخ مفتاح الـ `anon public`

---

## الخطوة 2: تجهيز الملفات للنشر (Build) 📦

1. افتح مشروعك في VS Code (أو Terminal).
2. اكتب الأمر التالي وانتظر حتى ينتهي:
   ```bash
   npm run build
   ```
3. سيظهر مجلد جديد اسمه `dist`. هذا المجلد يحتوي على موقعك "النهائي".

---

## الخطوة 3: رفع الموقع (Hosting) 🚀

1. اذهب إلى [netlify.com](https://netlify.com)
2. سجل حسابك مجاناً.
3. في لوحة التحكم، ابحث عن قسم يقول: **"Drag and drop your site folder here"**.
4. اسحب مجلد `dist` (الذي ظهر في الخطوة السابقة) وأفلته في المتصفح.
5. انتظر لحظات... ومبروك! موقعك أصبح على رابط (مثلاً `random-name.netlify.app`).

---

## الخطوة 4: الربط الأخير (Environment Variables) 🔗

الموقع يعمل الآن لكنه "فارغ" أو لا يسجل دخول. لنربطه بقاعدة البيانات:

1. في Netlify، اذهب إلى: **Site settings > Environment variables**.
2. اضغط **Add a variable**.
3. أضف المفتاح الأول:
   - Key: `VITE_SUPABASE_URL`
   - Value: (الرابط الذي نسخته من Supabase في الخطوة 1)
4. أضف المفتاح الثاني:
   - Key: `VITE_SUPABASE_ANON_KEY`
   - Value: (المفتاح الذي نسخته من Supabase في الخطوة 1)
5. ارجع لتبويب **Deploys** واضغط **Trigger deploy > Deploy site**.

---

## ✅ انتهى!
الآن موقعك أصبح:
- 🌍 **متاح للجميع** عبر الرابط.
- 📱 **يعمل على الجوال** والكمبيوتر.
- 💾 **يحفظ البيانات للأبد** في Supabase.
- 🔒 **آمن** وحسابات المدير والموظف تعمل بشكل حقيقي.
